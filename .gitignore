node_modules/
.firebase/
*.log
.DS_Store

# NOTA: public/config/client.config.js SÍ se commitea.
# Las claves web de Firebase son públicas por diseño (la seguridad real
# está en las reglas de Firestore + Auth), y GitHub Pages necesita el
# archivo en el repo para servir la app.
